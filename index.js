funtion theBeatlesPlay(musicians, instruments) {
  var array = [];
  for (var i = 0; i < musicians.length; i++) {
    array.push(musicains[i] + "plays" + instruments[i]);
      }
      return array; 
}